Within a paragraph of text, you can include code-like content by wrapping it in backticks. This will cause the code to be displayed in a monospaced font, and a coloured background will be applied to the code. This helps distinguish code from regular text. For example, we can refer to `my_function()` using the code:

```markdown
For example, we can refer to `my_function()` using the code:
```